{{#section 'title'}}
	Your Shopping Cart
{{/section}}
{{#section 'css'}}
<link rel="stylesheet" type="text/css" href="/css/detail.css"> 
{{/section}}
  
{{#section 'js'}}
    <script>
        $(".btn-down").click(function(){
            var input = $(this).siblings();
            var value = parseInt(input.attr('value'));
            var min = parseInt(input.attr('min'));
            if (value > min) {
                value--;
                input.attr('value', value);
                var spanElement = $(this).parent().parent().prev().find('span.amount')[0];
                var price = parseInt(spanElement.outerText.substr(0, spanElement.outerText.length - 2).replace(/,/g, ''));
                var oldTotal = parseInt($("#sum").attr('value'));
                var newTotal = oldTotal - price;
                $('#sum').attr('value', newTotal);
            }
            
        });

        $(".btn-up").click(function(){
            var input = $(this).prev();
            var value = parseInt(input.attr('value'));
            var max = parseInt(input.attr('max'));
            if (value < max) {
                value++;
                input.attr('value', value);
                var spanElement = $(this).parent().parent().prev().find('span.amount')[0];
                var price = parseInt(spanElement.outerText.substr(0, spanElement.outerText.length - 2).replace(/,/g, ''));
                var oldTotal = parseInt($("#sum").attr('value'));
                var newTotal = oldTotal + price;
                $('#sum').attr('value', newTotal);
            } 
        });
        
    </script>
{{/section}}


<div class="row product product-bottom">
    <div class="col-12 col-sm-8">
        <div class="row">
            <div class="col">
                
                <a href="/home"> << Mua thêm sản phẩm khác</a>
            </div>
            <div class="col text-right">
                <a>GIỎ HÀNG</a>
            </div>
        </div>
        <ul class="nav nav-tabs mb-3"></ul>
        {{#each items}}
        <div class="col-md-12 border ">                                                                                                                                                                                                                                                                                                                                                 
			
                <div class="row">
                    <div class="col-3">
                        <img href="/product/detail/{{Product.ProID}}" src="/img/{{Product.ProID}}/thumbnail.jpg" alt=""  height="150px" width="150px"></img>
                    
                    </div>
                    <div class="col-9">
                        <div class="row">
                            <div class="col-md-5 mt-3">
                                <p class="name">
                                    <a href="/product/detail/{{Product.ProID}}" class="effect-underline" style="color:black">{{Product.ProName}}</a>
                                    <div class="mt-5">Xóa</div>
                                </p>
                            </div>
                            <div class="col-md-3 mt-3">
                                <span class="font-weight-bold amount">{{number_format Product.Price}}
                                    <u>đ</u>
                                </span>
                            </div>
                            <div class="col-md-3 mt-3 ml-3">
                                <div class="btn-group" role="group">
                                    <button class="btn btn-light btn-down" type="button">-</button>
                                    <input type="tel" name="quantity" value="{{Quantity}}" min="1" max="{{Available}}" class="form-control input-quantity"
                                        style="display: block;">
                                    <button class="btn btn-light btn-up" type="button">+</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                    
        </div>
         {{/each}} 
    </div>
    <div class="col-12 col-sm-4">     
        <div class="new same-brand mt-4">
            <h4 class="title" style="background-color:#02A8F3; color:white">THÀNH TIỀN</h4>
            <div class="content">
                <div class="row">
                    <div class="col">

                    </div>
                    <div class="col">
                    <input class="text-right mr-3 borderless" id="sum" style="font-size:24px" value="{{sum}}"><strong>đ</strong></input>
                     <div class="text-right mr-3"><a>(Đã bao gồm VAT)</a></div>
                    </div>
                   
                
                </div>
               
        </div>
       
    </div>
     <div >
                          <button type="button" class="btn btn-large btn-success"></button>
                    </div>
            </div>
</div>